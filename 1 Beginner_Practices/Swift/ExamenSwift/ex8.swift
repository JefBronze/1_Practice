// Déclarer un tableau vide. 
var arr:[Int] = []

// Utilisation d'une boucle "for" pour itérer à partir du 10 jusqu'à 1
for num in (1...10).reversed(){
    arr.append(num)
}

// Afficher notre tableau
print (arr)
